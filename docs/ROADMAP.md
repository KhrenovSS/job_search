# История и состояние проекта

## Текущее состояние (снимок 2026-09-09 07:30 МСК)
- **Работает**: мост Claude `hh-scout-bridge` (systemd, opus); сервис `hh-scout` (systemd, enabled, код v5.1, перезапущен
  09.09 06:58, polling под ботом владельца, дайджест по cron 12:00, окно старта сбора 07:00–08:30).
- **Сегодня**: сбор не назначен (сервис стартовал после окна, `next_crawl_at` пуст) — до дайджеста возможен только `/crawl`.
  В 12:00 — первый автоматический дайджест: дооценка 7 загруженных описаний + отправка лидов с новыми кнопками; после
  него бот назначит утренний сбор на 10.09 (07:00–08:30).
- **Не сделано**: резервное копирование БД (этап 7); ни одной реальной обратной связи 👍/👎/✅ (петля и сворачивание
  карточек не проверены вживую); изменение дедлайна повторов (21:00) вступит в силу при следующем рестарте сервиса.
- **БД**: 387 вакансий (378 карточек поиска + 9 заглушек по откликам владельца) — `to_fetch` 110, `prefiltered` 7,
  `sent` 9, `rejected` 13, `skipped` 248 (правила 165 + триаж 81 + 2 архив/отклик); 22 оценки, 9 писем, 0 отзывов,
  0 действий по лидам; `runs`: #1 и #2 прерваны внешне, #3 (описания, 30 загрузок) остановлен вручную ради установки
  сервиса. 9 `sent` — превью, показанные владельцу через `evaluator --send` и помеченные при первом старте бота
  (`kv.preview_marked`); кнопок у них нет.
- **Git**: github.com/KhrenovSS/job_search (public, MIT), ветка `main`. Локальные файлы вне git: `.env`, `bridge/.env.bridge`,
  `prompts/candidate_profile.md`, `prompts/resume.md`, `docs/archive/2026-09-08-initial-vision/` (PDF резюме, переписка,
  старые доки), `data/`. Образцы: `prompts/*.example.md`.

## v5 — жизненный цикл лида в чате ✅ код (2026-09-08 22:40, требует перезапуска сервиса)
Кнопки ✅ Написал / ⏸ Позже рядом с 👍/👎; закрытые лиды сворачиваются в строку, письмо удаляется; автозакрытие по откликам
на hh.ru после сбора; `/inbox`, `/done`, `/cleanup`; таблица `lead_actions`, миграция `_m005`.

## v5.1 — утренний сбор ✅ код (2026-09-09 06:50, требует перезапуска сервиса)
`CRAWL_WINDOW=07:00-08:30` (старт сбора утром, назначается после дайджеста на следующий день); перед дайджестом 12:00
дооцениваются все загруженные описания и дописываются письма (`bot/digest._evaluate_pending`), чтобы утренний сбор
попадал в тот же дайджест хотя бы частично. Сегодня (09.09) сбор не назначен — до дайджеста только `/crawl`.

## Открытые дела (по приоритету)
1. Пронаблюдать первый автоматический цикл: дайджест 2026-09-09 12:00 → `/next` → утренний сбор 10.09 → отчёт → дайджест.
2. Этап 7: ежедневный `sqlite3 .backup` в `data/backups/` (7 копий), `/status` с датой бэкапа.
3. Проверить петлю обратной связи на реальных 👎: после нескольких похожих отказов баллы должны снижаться.
4. По результатам недели: подстроить `TITLE_REQUIRED_ANY`, порог 60, веса, длину писем (сейчас выходит 1600–1900 знаков при цели 1000–1500).

## Как менялась цель (см. `docs/DECISIONS.md`)
v1 (утро 8.09): штатная работа через API hh.ru с OAuth → v2: API закрыт, Firefox + Marionette, хост без Docker →
v3: вакансии = лиды для сотрудничества по ИП, веса техника/роль/лид → v4: 49 регионов европейской части + письмо-отклик
под каждым лидом → этап 5: бот, расписание, оркестратор → v5: жизненный цикл лида в чате (✅/⏸, сворачивание,
автозакрытие) → v5.1: утренний сбор 07:00–08:30 с дооценкой перед дайджестом.

## Этапы (2026-09-08 … 09-09)
| Этап | Что | Статус | Проверено |
|---|---|---|---|
| 0 | Каркас: config, db, логирование; мост `bridge/` | ✅ | мост отвечает на sonnet/opus; тесты |
| 0b | Перевод на хост: systemd-юниты, geckodriver, `setup_firefox.sh`, `session.py` | ✅ | attach/detach к headless и к реальному Firefox |
| 1 | Страницы hh.ru: URL, парсеры `HH-Lux-InitialState`, зарплаты, паузы | ✅ | 50 карточек с живой страницы; тесты на фикстурах |
| 2 | Сбор: 3 прохода, серии, дедуп, отклики, `suitableVacancies` | ✅ | 15 загрузок → 373 новых; повтор 5 → 5 новых; оба прогона прерваны внешне, восстановление отработало |
| 3 | Правила → триаж ИИ → описания; лимит 80/день | ✅ | 378 → правила отсеяли 165 → триаж открыл 134/213; описания сериями |
| — | Инфраструктура: мост как сервис, Telegram-бот, chat_id | ✅ | `/complete` из-под systemd за 9 с; подтверждение в Telegram |
| 4 | Оценка ИИ как лид (техника/роль/лид), карточка, превью | ✅ | 22 оценки, 9 лидов ≥ 60, превью в Telegram одобрено владельцем |
| v4 | 49 регионов; письма-отклики | ✅ | 9 писем, формат одобрен («так и нужно») |
| 5 | Оркестратор, планировщик, бот, `main.py`, тесты | ✅ | бот отвечает на команды; сервис установлен 08.09 21:46 |
| 6 | Петля обратной связи (feedback_block в промпте) | ✅ код | на реальных отзывах не проверено |
| v5 | Жизненный цикл лида: ✅/⏸, сворачивание, автозакрытие, /inbox /done /cleanup | ✅ код | сервис перезапущен 09.09; кнопки вживую — с дайджеста 09.09 12:00 |
| v5.1 | Утреннее окно 07:00–08:30, дооценка перед дайджестом, дедлайн повторов 21:00 | ✅ код | первый утренний сбор — 10.09 |
| 7 | Надёжность: бэкап БД (⏳ не начат), README (✅ есть) | частично | — |

## Уроки
- Фоновые задачи из сессии Claude Code убивает её сторож памяти — долгие прогоны только `setsid`/systemd.
- Стоп-слова матчить по началу слова («водитель» ≠ «руководитель»).
- Первый вариант весов (деньги/формат) скрывал лучшие лиды — критерии должны следовать цели владельца, а не резюме.
- `.env` для systemd: комментарии только отдельными строками.

## Идеи на потом (не делать без запроса владельца)
- Еженедельная сводка: сколько лидов, какие компании, ответы.
- Напоминание о follow-up через N дней после «✅ Написал», если ответа не было.
- Расширение поиска на другие площадки.
