# Ключевые решения и их причины

| # | Решение | Почему | Дата |
|---|---|---|---|
| 1 | **Браузер владельца (Firefox + Marionette) вместо API hh.ru** | hh.ru закрыл API для соискателей 15.12.2025; анонимный `GET /vacancies` отвечает 403. Страницы сайта содержат полный JSON `HH-Lux-InitialState` — парсим его, не вёрстку | 2026-09-08 |
| 2 | **Вакансия = лид для сотрудничества по ИП, а не работа** | Владелец работает по ИП и ищет подработку/проекты. Первый срез с весами «деньги/формат» скрыл технически идеальные лиды (один интегратор: 46 → 89 после переформулировки). Зарплата и формат — только факты | 2026-09-08 |
| 3 | **Веса 0.55 техника / 0.25 роль / 0.20 лид, порог 60; total считает код** | Веса можно менять без перепромптинга и пересортировывать старые оценки. Роль отделяет программиста от проектировщика/эксплуатации; лид — насколько компании удобно предложить подряд | 2026-09-08 |
| 4 | **Триаж ИИ по карточкам перед загрузкой страниц** | Каждая страница вакансии — загрузка в браузере под дневным лимитом (тогда 80, с v6 — 100–140). Триаж (30 карточек за вызов, hh.ru не видит) сократил открываемые страницы примерно вдвое | 2026-09-08 |
| 5 | **Осторожность: серии 3–7 страниц, паузы 10–40 мин, ≤ 80 загрузок/день, один плановый сбор в сутки в случайное время** (ручной `/crawl` или рестарт сервиса могут добавить второй — бюджет общий) | Требование владельца: «максимально аккуратно, чтобы аккаунт не заблокировали», анализ можно размазать на часы | 2026-09-08 |
| 6 | **Дайджест ежедневно в 12:00, старт сбора — случайное время в окне 07:00–08:30** (было 13:00–23:00) | Фиксированное время активности выглядит роботом; результат нужен ежедневно в предсказуемый момент. Владелец попросил собирать с утра, чтобы дайджест содержал свежие вакансии того же дня; перед дайджестом дооценивается всё уже загруженное | 2026-09-09 |
| 7 | ~~**География: 49 регионов европейской части без Урала, Кавказа и новых территорий**~~ | Владелец готов к удалённой работе и выездам на ПНР в разумном радиусе (~1300 км). **Отменено 13.09 решением №25** | 2026-09-08 |
| 8 | **Свой мост Claude (`bridge/`), не общий с другими проектами хоста** | Проект независим от running-coach: свой порт, токен, модель (opus). Мост — `claude -p` под подпиской, API-ключа нет | 2026-09-08 |
| 9 | **Хост + systemd, без Docker** | Браузер живёт в X-сессии хоста; Docker Hub с хоста недоступен; Docker не добавлял ничего | 2026-09-08 |
| 10 | **aiogram 3 + SQLite без ORM** | Один пользователь, прозрачность важнее масштабируемости | 2026-09-08 |
| 11 | **Письмо-отклик под каждым лидом, факты только из резюме, явное предложение по ИП** | Владелец копирует письмо из Telegram и отправляет сам; стиль — его лучшее письмо (пример про AGV) | 2026-09-08 |
| 12 | **Долгие прогоны — только отсоединёнными процессами или под systemd** | Фоновые задачи из сессии Claude Code убиваются её сторожем памяти (случилось дважды) | 2026-09-08 |
| 13 | **Жизненный цикл лида в чате: закрытые карточки сворачиваются в строку, письмо удаляется; 👍/⏸ не закрывают; автозакрытие по откликам на hh.ru; устаревание только по `/cleanup`** | После нескольких дней лента Telegram — стена ссылок без пометок; владелец не помнит, где остановился. Свёрнутые строки сохраняют историю, удаление письма экономит место (текст остаётся в БД). Автоматического устаревания владелец не захотел. Решения владельца 2026-09-08 22:20 | 2026-09-08 |
| 14 | **Три подхода в день (07–10 / 12–15 / 18–22), серии по времени ~7–13 мин с паузами ~4–9 мин, дневной лимит случайный 100–140, доля лимита на подход, приоритет 3 триажа списывается через 3 дня, ретраев внутри дня нет** (заменяет №5 и №6) | Владелец: «мало вакансий, пусть листает постоянно, 10 минут листает / 5 тишины». Разбор: поиск дешёвый (9 задач, 373 карточки за 15 загрузок), дорого — страницы вакансий (1 загрузка = 1 вакансия), очередь `to_fetch` 110 + 30–50 новых в день не влезала в 80/день; паузы 10–40 мин объём не ограничивали — ограничивал лимит и один подход. Ровный такт 16 часов подряд — главный признак робота, поэтому дневные окна, разброс всех интервалов, случайный лимит, «чтение» 6–20 с; активный соискатель смотрит 100–150 вакансий в день. Все 11 лидов вышли из приоритета 1, а 43 карточки приоритета 3 занимали очередь. Ретраи не нужны: следующее окно через несколько часов. Решения владельца 2026-09-09 19:00 | 2026-09-09 |
| 15 | **Тревоги в Telegram: сторож расписания каждые 30 мин, предпроверка за 30 мин до подхода, разбор отчёта на признаки блокировки/смены разметки, итог дня, systemd OnFailure → curl; каждая не чаще раза в день** | 09.09 сервис весь день не собирал вакансии и молчал: сообщения были только об исходе сбора, не о его отсутствии. Владелец: «хотелось бы этого избежать, присылать оповещение, что что-то идёт не так». Дедупликация — чтобы постоянная причина не превращалась в спам | 2026-09-09 |
| 16 | **Рестарт сервиса без пароля через узкое sudoers-правило (`/etc/sudoers.d/hh-scout`) и обёртку `scripts/svc.sh`, которая отказывается перезапускать во время сбора** | Каждое обновление кода требовало от владельца `sudo systemctl restart` руками; ИИ-агент работает под тем же пользователем и без пароля sudo не имеет. Правило перечисляет точные команды (`systemctl` для двух юнитов, `daemon-reload`, `install` юнитов из `data/`), а не `ALL`; проверка `visudo` с откатом. Охрана рестарта — потому что Marionette держит одну сессию и рестарт посреди подхода убьёт прогон. Образец — такое же правило для моста в проекте running-coach | 2026-09-09 |
| 17 | **Подход ограничен окном: старт не позже чем за 60 мин до конца окна, дедлайн браузера — конец окна + 30 мин, неизрасходованная доля лимита сгорает; подход, чьё окно уже закрылось (восстановлен после простоя), пропускается** | Первый подход v6 (run #4, 09.09) стартовал в 21:51 в окне до 22:00, получил весь дневной лимит 111 и листал hh.ru до 23:49 — ночью, что противоречит правилу «только дневные окна» и README «ночью бот молчит». Единственный дедлайн был у серии. Лимит — потолок, а не план: недобор безопаснее ночной активности. Оценка и письма после дедлайна идут (мост, не браузер) | 2026-09-10 |
| 18 | **profi.ru как второй источник — через браузер владельца, при минимальном следе (1 загрузка ленты за подход, только чтение, стоп при капче), выключен по умолчанию; доставка заказов сразу после прогона** | Заказы видны только специалисту после входа, публичные страницы пусты, API нет; кабинет — JS без JSON. Правила profi.ru прямо запрещают парсинг с правом удалить аккаунт, отклики платные — владелец предупреждён и выбрал браузер (вариант с почтовыми уведомлениями + IMAP на Mail.ru оставлен планом Б). Заказов единицы и их разбирают за часы — отправлять в 12:00 бессмысленно, поэтому мгновенная отправка; триаж ИИ не нужен, все идут в оценку. Карточка ленты содержит полный текст — страницу заказа не открываем, экономя лимит и след. `hh_id='profi:<id>'` вместо новой таблицы: весь жизненный цикл лида (кнопки, письма, /inbox) ключуется на `vacancies.id` и переиспользуется без правок | 2026-09-10 |
| 19 | **Тихий режим: служебные сообщения только о сбоях; единственный суточный отчёт — дайджест 12:00 со строкой «Работа за сутки»; отчёт планового подхода — только при `not ok` и без тревоги, «Итог дня» — только ⚠️ при недоборе; `/crawl` подробный** | Владелец 10.09: «присылать сообщения о работе приложения только если что-то идёт не так; если всё штатно — пусть молча работает и раз в сутки отчитывается». При штатном дне приходило 8–10 служебных сообщений (старт/конец трёх подходов, итог дня, «запущен» на каждый рестарт). Канал «что-то не так» уже есть (№15), дублировать его отчётами не нужно; тревога с подсказкой информативнее отчёта, поэтому при обоих остаётся тревога. Рестарт — штатная операция (в т.ч. агентом), падение ловит OnFailure. Флаг «болтливого режима» не заведён: для отладки хватает `/crawl` и journald | 2026-09-10 |
| 20 | **Одна компания — один лид: вакансии работодателя, уже получившего лид (отправлен за `EMPLOYER_REPEAT_DAYS`=90 или ждёт дайджест), пропускаются как `duplicate_employer` как можно раньше (до триажа ИИ и до загрузки страницы), среди оценённых остаётся лучшая по баллу; ключ — hh.ru `company.id` (новая колонка `employer_id`), для карточек без id — имя** | Владелец 10.09: «некоторые компании размещают схожие вакансии в разных регионах. Достаточно дать 1 вакансию, я пишу HR-менеджеру и если он заинтересован — сотрудничество будет со всей организацией, а не по отдельным регионам». В БД «РУССКИЙ ПРОДУКТ» дал 5 лидов в 5 городах за один дайджест. Ранний отсев экономит лимит загрузок и вызовы моста; сравнение по id надёжнее имени, но старые карточки id не имеют — потому оба ключа и транзитивная группировка. 90 дней — компромисс: спустя квартал у компании могла смениться ситуация и HR, письмо уместно снова; 0 — навсегда. profi.ru исключён: там `employer` — имя частного клиента | 2026-09-10 |
| 21 | **Соединения SQLite в автокоммите (`isolation_level=None`), пакетные записи — явно через `db.transaction()` (`BEGIN IMMEDIATE`), `synchronous=NORMAL` в WAL; отметка сторожа в kv при `database is locked` — warning, не тревога** | 11.09 21:55 сторож упал с «database is locked»: префильтр 224 карточек шёл 21 с, потому что `with conn:` при `isolation_level=None` транзакцию не открывает и каждый UPDATE коммитился отдельно с fsync на HDD (~90 мс); busy-handler второго соединения спит до 100 мс между попытками и каждый раз попадал на очередной коммит. Переводить все соединения в неявные транзакции опасно: любая запись вне `with` держала бы блокировку до конца сессии. Поэтому автокоммит остаётся, а горячие циклы (префильтр, дедуп, сохранение карточек, откликов) обёрнуты явно; nested-вызов присоединяется к внешней транзакции. `NORMAL` в WAL безопасен при падении процесса (теряется только при отключении питания — последние коммиты). Отметка `watchdog_last` — учёт, не проверка: её потеря на 30 мин не стоит сообщения владельцу | 2026-09-11 |
| 22 | **Письмо всегда объясняет модель оплаты: ориентир в резюме — про штатную позицию, по договору оплачивается объём (этап, задача, согласованные часы); сумм в письме нет никогда, ни своих, ни из вакансии** | Компании отвечали «зарплата у нас значительно скромнее, чем Ваши финансовые ожидания», хотя предлагался подряд, а не наём. Разбор 50 отправленных писем: **ни одной суммы ни в одном** — возражение приходило не из письма, а из шапки резюме на hh («270 000 ₽ на руки», месячный оклад штатника). У вакансий, куда уходили письма, медиана `salary_from` 120 000 ₽ и максимум 217 500 ₽ — их цифра почти всегда ниже, и разговор закрывался до предложения по ИП. Цифру в резюме владелец оставил намеренно: она нужна как якорь для штатных предложений, поэтому развязку делает письмо. Абзац обязателен даже когда вакансия молчит о деньгах (42 % таких), а зарплата вакансии передаётся в промпт **флагом `salary_stated`, а не числом** — чтобы модель физически не могла процитировать сумму | 2026-09-12 |
| 23 | **Письмо предлагает только сотрудничество по ИП: оговорка «штатный найм не исключаю» убрана везде, включая подчёркнуто штатные вакансии; обе суммы — и ориентир в резюме, и вилка в вакансии — объявляются месячным окладом штатного сотрудника, а стоимость договора считается от объёма, сроков и состава задач** | Владелец 12.09: «важно показать, что я хочу работать по ИП; указанная у меня сумма в месяц, равно как и сумма в описании вакансии, не может относиться к ИП, потому что тут договорные отношения». В письмах v8.3 намерение не читалось: абзац заканчивался «штатную позицию тоже не исключаю», предложение выглядело запасным вариантом, а про вилку вакансии говорилось «её рамки сотрудничеству не мешают» — то есть согласие на их сумму вместо перевода разговора в договорную плоскость. Выбран мягкий вариант формулировки («считается иначе»), а не «неприменимы»: тон констатации, не спора. Демонстративный отказ от штата тоже запрещён — письмо просто не поднимает эту тему. Штатная вакансия при этом остаётся полноценным лидом (решение №20 и профиль § 21): меняется только то, что письмо предлагает компании | 2026-09-12 |
| 24 | **Приоритеты отклика: блок «По вашим задачам» (4–6 пунктов) — ядро письма и сокращается последним; обязательная гибкость одной-двумя конкретными фразами (состав работ/этапы/сроки под их график, можно начать с одного узла или этапа, работа в их процессе); деньги — ровно одно предложение внутри абзаца про сотрудничество, отдельного блока нет; потолок длины 2800 → 3200** | Владелец 12.09: «ориентир по ценам давать не нужно; в отклике важно показать схожий с вакансией опыт, готовность сотрудничать и гибкость — готов разговаривать, обсуждать условия сотрудничества по ИП». Прежний промпт делал обратное: правило «если не укладываешься, сокращай пункт 3» жертвовало совпадением опыта ради денежного абзаца на два предложения, а слов о готовности обсуждать условия не было вовсе — «готов обсудить» появлялось только у подчёркнуто штатных вакансий. Гибкость формулируется конкретикой, а не словами «гибок и открыт», и подаётся как снятие риска для компании, а не как уступка. Форма оформления в список обсуждаемого намеренно не входит: формат именно ИП (решение №23). Лимит поднят, потому что расширенный блок опыта законно удлинил письма до ~2750 знаков — потолок ловит разговорчивость модели, а не длину как таковую | 2026-09-12 |
| 25 | **Поиск по всей России одним запросом (`SEARCH_ALL_RUSSIA`, area=113) + два запроса по брендам (Siemens/TIA/WinCC/ОВЕН/Beckhoff…) и протоколам (Modbus/OPC UA/диспетчеризация/АСКУЭ); порог 60 и правила префильтра не тронуты** (заменяет №7) | 13.09 дайджест пришёл пустым. Разбор: периметр вычерпан за четыре дня — за подход бот видел **393 карточки, новых из них 26** (93 % уже в базе), приток упал 566 → 411 → 348 → 15 → 49 в сутки. Дневной лимит был ни при чём: израсходовано **37 загрузок из 101**, потому что кончались не страницы, а *задачи* — 12 штук (3 запроса × 4 прохода), и каждая обрывалась по правилу «страница без новых → дальше всё известное». Правила проверены поимённо на отсеве 13.09 (руководитель бетонного производства ×7, мастера электромонтажа вахтой ×9, слесари КИПиА, DevOps, проектировщики) — ложных отбраковок нет, поэтому смягчать нечего; в зоне 55–59 за шесть дней всего 12 оценок, порог не главный рычаг. Расширение географии бесплатно по бюджету: все регионы уходят в hh одним URL отдельными `area`, а работа по ИП удалённая — регион заказчика роли не играет (отсюда отмена №7). Окно `SEARCH_PERIOD_DAYS=2` оставлено: вакансии старше уже в базе, увеличение дало бы перезагрузку известного, а не приток | 2026-09-13 |
| 26 | **Доля бюджета подхода резервируется под страницы вакансий (`DETAILS_BUDGET_SHARE`=0.4): сбор получает `budget − резерв`, описания — резерв плюс всё несобранное; нулевой `hh_budget` из-за резерва больше не считается исчерпанным лимитом** | Прогон #14 13.09: сбор потратил 15 загрузок из 15, `DetailsFetcher` получил 0 («Описаний к загрузке: 0, бюджет 19»), ни одной страницы вакансии не открыто — карточки собраны, оценивать нечего. При узком периметре это было безобидно, но с 20 задачами и `MAX_PAGES_PER_QUERY`=6 сбор в пределе запрашивает 120 страниц при лимите 100–140, то есть стало бы штатным поведением и обнулило бы расширение периметра. Резерв — **пол, а не потолок**: если открывать нечего, он просто не тратится, дневной лимит считается по фактическим загрузкам и остаток переходит в следующий подход | 2026-09-13 |
| 27 | **Сетевая ошибка profi.ru не роняет прогон: уходит в `profi_error` (мягкая тревога), а не в `errors`; текст сокращён до класса исключения** | С 12.09 каждый прогон помечался `failed` и слал тревогу, хотя hh.ru отрабатывал полностью (прогон #15: 26 новых, 6 оценок, 1 лид). Причина — `profi.ru` перестал резолвиться через DNS роутера (через 1.1.1.1 резолвится); общий `except` клал `dnsNotFound` в `errors`, а многострочный стектрейс Marionette уезжал целиком в `runs.error` и в текст тревоги. profi.ru — вторичный источник по построению (решение про минимальный след), ронять из-за него основной поток нельзя; `health.py` уже умеет мягкую тревогу «hh.ru это не затронуло», так что реальная поломка не теряется | 2026-09-13 |
| 28 | **`tech_score` получил числовые полосы; бренд ПЛК сам по себе не штрафует: перечень из нескольких платформ в требованиях — норма (80–100), штрафует только единственная обязательная чужая среда (40–60), а «не ПЛК/SCADA вообще» — 0–30** | Владелец 14.09 (разбор `137255912`, ФомЛайн): «снижать рейтинг, если упор идёт явно не на ПЛК на базе CODESYS или Schneider; если указано, что нужно знать несколько брендов — это норма». У `tech_score`, в отличие от `role_score`, полос не было — только список «ОВЕН, Schneider, Siemens, Beckhoff — нормально», и модель дрейфовала: штрафовала за любой бренд вне списка и даже за Siemens, который в списке есть («TIA Portal обязателен, не основная специализация»). Из 360 отклонённых 25 набрали ≥ 50; у трёх с `role ≥ 70` в требованиях стоял **перечень** платформ (Beckhoff+TIA+ОВЕН; TIA+Регул; Регул+Step7+WinCC+ТЕКОН) — это компании, ищущие общее умение программировать ПЛК, то есть настоящие лиды. Полоса 40–60 (а не 0–30) намеренно не приговор: при сильных `role` и `lead` вакансия на чужой среде ещё проходит порог. Сам ФомЛайн (единственная обязательная среда — Allen-Bradley) остаётся отклонённым — это и есть верный ответ | 2026-09-14 |
| 29 | **`duplicate_employer` стал обратимым: `dedup.revive_orphans` в начале каждого прогона возвращает в очередь дубли, чей «победитель» так и не стал лидом** | Дубль гасится против вакансии в статусе `prefiltered` — то есть **до** оценки (так задумано: экономит загрузку страницы и вызов моста). Но если та потом уходит в `rejected`/`evaluation_failed`, компания выпадала целиком и её копии не рассматривались больше никогда: в базе 18 таких строк из 23 дублей. На ФомЛайн это безвредно (копии в Москве и Кузнецке идентичны), но в общем случае теряет лид молча — у компании могла быть вторая, более подходящая вакансия. Возврат идёт на ту стадию, которой строка достигла (`to_fetch`, если триаж ИИ пройден, иначе `triage`), и не зацикливается: воскрешённый дубль после оценки становится `rejected`, а не `skipped`. «Лидом не стал» решает `covering_lead`, а не статус: `evaluated` ниже порога потерян так же, как `rejected`, — иначе дубль ждал бы до ближайшего дайджеста; статус смотрится только чтобы не будить дубль, пока «победитель» ещё в пути (`new`/`triage`/`to_fetch`). Компания с любым другим живым или отправленным лидом дублей не получает | 2026-09-14 |
| 30 | **Оценка ищет не совпадение резюме, а отделяемый кусок работы: п.1 промпта оценки стал двухшаговым (есть ли спрос → что из требований можно взять по ИП, разово или вдолгую), `pitch_hint` обязан называть этот кусок их словами; несколько обязательных брендов одновременно — норма наравне с «на выбор»** | Владелец 14.09: «несколько обязательных брендов одновременно — тоже норма; задача: изучить требование работодателя и найти моменты, в которых сотрудничество с моим ИП будет для него полезно, будь то разовое сотрудничество или на длительной основе». Полосы v8.7 (решение №28) отвечали на вопрос «это мой стек?», а нужен ответ «что здесь моё?»: у вакансии на чужой платформе верхний уровень, SCADA/HMI, интеграция, БД и архивы, ПНР программной части остаются доступными, и письмо должно предлагать именно их. Пример — `136843635` (ТЕКОН + REGUL + MKLogic + Siemens обязательны одновременно): формально «перечень брендов», но модель прочла это как «обязателен опыт специфичных ПТК» и дала `tech 55`. Полосу 40–60 за единственную обязательную чужую среду решение №28 сохраняет — меняется не строгость, а то, что модель ищет | 2026-09-14 |
| 31 | **Чужая единственная среда больше не топит `tech_score`, если в требованиях есть реальный отделяемый кусок под ИП: полоса 60–75 вместо 40–60; чистые 30–50 остаются только когда взять нечего** (уточняет №28) | Владелец 14.09, на прямой вопрос: «лид, если кусок реальный». Правки №28 и №30 на границе тянули в разные стороны: полоса 40–60 за единственную чужую среду держала вакансию под порогом, а новый двухшаговый п.1 находил в той же вакансии внятный повод написать. На ФомЛайн `137256632` (Allen-Bradley) это было видно дословно: `tech 45`, балл 56 — и при этом `pitch_hint` «SCADA/HMI-экраны, архивы и отчёты, обмен по Modbus/OPC, БД на SQL, доработка и сопровождение — отдельными задачами по договору ИП». Решает теперь не бренд, а есть ли работа, которую кандидат может взять; «реальный» кусок — названный в их требованиях, а не придуманный. Побочное наблюдение: на границе оценка шумит — `136460663` за три прогона одного текста дала 59, 60 и 55, поэтому «ровно порог» ничего не гарантирует и полосы важнее десятых долей | 2026-09-14 |
| 32 | **Досье на компанию из открытых источников собирает сам CLI моста (`allow_web`, только `WebFetch`/`WebSearch`), а не браузер владельца; одно досье на компанию в таблице `employers`; факты и предположения разделены; письмо получило строку о производстве, отраслевой мост к резюме и абзац для кадровика** | Владелец 14.09: «сделать отклик ещё более персонифицированным, именно под конкретную компанию; зацепить менеджера по кадрам, чтобы он предложил руководителю мой вид сотрудничества; поискать информацию по открытым источникам». Письмо знало о компании только название — в `letter_payload` не было даже города. Разведка: `api.hh.ru/employers/{id}` отдаёт 403 (справочник `/areas` при этом открыт), то есть публичного API по компаниям нет; страница работодателя на hh.ru богата (ФомЛайн: «30 лет ключевой производитель эластичного пенополиуретана», отрасль, сайт, 7 заводов); а `claude -p --tools "WebFetch,WebSearch" --max-turns 6 --model sonnet` прочитал её сам за 2 хода и $0.05. Поэтому разведка идёт **мимо браузера владельца**: дневной лимит 100–140 загрузок не тратится (а он выбирается почти полностью — 101–139), сессия Marionette не занимается, на его IP следов нет. Кэш по `employer_id` — тому же ключу, что у «одна компания — один лид», так что платим раз на компанию (~8–11 в день, $0.4–0.6). Сайт компании открывается не всегда (foamline.com молчит) — поэтому `found: false` штатный исход и хранится, чтобы не платить дважды. Жёсткое разделение «прочитал / предполагаю» плюс `sources` в БД и строка «🏭 О компании» в карточке — защита от выдумок про малоизвестные компании: владелец видит основание письма до отправки. Абзац для кадровика — «прямо, но без давления» (решение владельца): задача поедет без ставки в штате, можно начать с одного узла, подряд не мешает поиску штатного; запрещены «помогу закрыть позицию», намёки на долгий поиск и любое давление | 2026-09-14 |
| 33 | **Пять продуманных писем в день из сквозной очереди вместо всех лидов подряд: приоритет = балл + 1 за сутки ожидания (потолок 7), непрошедшие норму не отбрасываются и ждут, срок жизни в очереди 30 дней; письмо проходит проверки кодом и второй проход редактора** | Владелец 14.09: «лучше откликнуться на 5 вакансий письмом, которое очень продуманно и на которое будет реакция, чем разослать 20, которые проигнорируют»; на предложение просто поднять порог ответил иначе — нужен рейтинг, где сильные идут первыми, а слабые ждут своей очереди и дожидаются, когда сильные кончатся. Данные это подтверждают: доля ответов работодателя (`has_chat`) среди вакансий, куда владелец написал, — 75+ 61 % (11/18), 70–74 43 % (3/7), 65–69 33 % (1/3), **60–64 0 % (0/3)**; у 38 👍 средний балл 77,4, у 18 👎 — 65,3 при максимуме 74. Порог 60 оставлен как входной фильтр в очередь (ниже — мусор), а отбор идёт по приоритету: так «немного не дотянувшие» видны в хвосте дайджеста и к ним можно попросить письмо, а не пропадают. Бонус за ожидание ограничен семью баллами, иначе старая слабая вакансия однажды обошла бы любую сильную. Норма суточная, а не на прогон: сбор идёт трижды в день и иначе дал бы 15 писем. Освободившиеся деньги ушли в глубину — разведка до 6 ходов и 5 страниц, проверки кодом (суммы, штампы, названо ли производство) и проход редактора по чеклисту; итого ~$1.5 в день против ~$3 | 2026-09-14 |
| 34 | **Разведка по компании переезжает с sonnet на opus (модель моста), глубина возвращается к 5 ходам и 4 обращениям к сети, а у разведки появляется собственный клиент моста без ретраев** | Владелец 14.09: «оставь текущую модель opus, у меня корпоративная подписка, и я трачу её; важнее качество отклика и проработанность писем» — деньги перестали быть ограничением, а разведка решает, насколько письмо понимает производство компании. Одновременно найдена настоящая причина утренних 21 минуты и $0.60 на ВОЛЬТЕР, из-за которых глубину откатили в v9.1.2: `CompanyResearcher` задуман как «одна попытка» (`BridgeClient(settings, retries=0)`), но `CoverLetterWriter.__init__` передавал разведке **свой** клиент с `retries=2` и умолчание молча затиралось. Недоступный сайт → мост отдаёт 504 через `BRIDGE_WEB_TIMEOUT` = 420 с → ещё два повтора: 3 × 420 = 1260 с, в логе 1253 с. То есть откат глубины лечил симптом, а не причину: глубина стоила секунд, ждала не она. После починки худший случай одной разведки — один таймаут 420 с, поэтому 5 ходов / 4 обращения безопасны; дальше сознательно не идём — при `COMPANY_RESEARCH_MAX_PER_RUN` = 5 худший случай шага писем и так 35 минут, а дайджест ждать не должен. Правило «сайт не ответил — второй раз не пробуй» из v9.1.2 остаётся | 2026-09-14 |
| 35 | **Отклик закрывает компанию на `EMPLOYER_REPEAT_DAYS` (90 дней): её новые вакансии не становятся лидами (`skipped/employer_responded:<hh_id>`), письма ей не пишутся ни в дайджесте, ни по `/letter`** | Владелец 14.09: «переписывать нужно отклики только в те компании, в которые я ещё не откликался; если я уже отправил отклик, то нет смысла переписывать текст отклика». Правило держалось наполовину: `employer_lead` закрывал компанию, только если её вакансия попала в дайджест (`sent`) или ждала его, — а отклик, отправленный владельцем прямо на hh.ru, бот видит в «Откликах» и ставит `applied=1`, но на дедуп это не влияло. Таких компаний в базе было 39, из них 9 никогда не были в дайджесте: появись у такой компании новая вакансия — стала бы лидом и получила письмо, хотя тому же кадровику уже писали. Срок выбран тот же, что у дайджеста (владелец: «90 дней, как `EMPLOYER_REPEAT_DAYS`»), чтобы правило было одно, а не два: через 90 дней у компании обычно и проекты, и вакансии другие. Причина пропуска **отдельная** от `duplicate_employer:` именно потому, что `dedup.revive_orphans` воскрешает дубли, а компанию с откликом воскрешать нельзя. Проверка стоит единой точкой в `CoverLetterWriter.answered_employer`, до разведки и черновика, — значит держится и в дайджесте, и в `/letter`, и в любом ручном скрипте; `/letter` при этом не молчит, а отвечает «вы уже откликались … <дата>» (владелец выбрал отказ, а не предупреждение) | 2026-09-14 |
| 36 | **Разведка больше не упирается в сайт компании: молчащий адрес — штатный случай, факты берутся из открытых источников и из других вакансий этой же компании на hh.ru; потолок 6 обращений к сети** | Владелец 14.09, получив дайджест: «изучи компании, найди зацепки и сделай отклики персонифицированными». Разбор десяти лидов: **девять досье из десяти собраны вообще без сайта** — nt-ls.ru, gtexport.ru, foamline.com, g-mach.ru, industrialsystems.ru, inctrl.ru, ditec.ru, av-automatics.ru не ответили, sibatomservice.ru не резолвится; прочитан сайт только у НПО ТЕХНОИНТЕГРАЦИЯ и (утром) у ВОЛЬТЕР. Проверка с хоста: TCP 80/443 у тех же адресов молчит даже у volter.ru, который мост читал днём, — значит недоступность системная, а не дело промпта или модели. А промпт был написан так, будто сайт единственный источник: «сайта нет — один поиск, больше не ищи» и прямой запрет открывать вакансии. Он и обрубал лучший доступный источник: вакансии компании читаются всегда и называют бренды ПЛК, SCADA, протоколы и типы объектов прямее любого сайта. Теперь: одна попытка `https` + одна `http` (ВОЛЬТЕР читался именно по http), затем 2–3 поиска по каталогам, порталам, выставкам и тендерам, затем 1–2 чужие вакансии этой же компании; `note` обязан сказать, откуда что взято, `found: false` — только когда не узнали ничего сверх названия. Потолок поднят с 4 до 6 обращений (`COMPANY_RESEARCH_MAX_TURNS` 5 → 8): после починки ретраев (v9.2) глубина стоит секунд, а не минут. В `prompts/cover_letter.md` заодно запрещено переносить в письмо обтекаемые обороты из `automation_hooks` («у такого производства обычно…», «ваш профиль … мне близок») — именно они делали письма безликими | 2026-09-14 |
| 37 | **Дневной лимит загрузок 100–140 → 150–200, доля под страницы вакансий `DETAILS_BUDGET_SHARE` 0.4 → 0.55; ритм человекоподобия не меняется** | Владелец 14.09 разобрал за день все 15 присланных лидов и выбрал ускорить поиск числом открываемых страниц. Лимит оказался именно связывающим, а не формальным: при норме дня 103 подходы взяли 35 и 34 загрузки, то есть каждый выбирает свою долю до конца, и рост лимита прямо превращается в открытые страницы. Второе узкое место в тех же числах: за подход собирается 243 карточки против 14 открытых страниц вакансий — карточки никогда не были дефицитом, а лид рождается только из открытой страницы, поэтому резерв под описания поднят до 0.55. Серии 7–13 мин, паузы 4–9 мин, чтение 6–20 с и три дневных окна остались прежними: растёт объём, а не «роботность» такта — подход 13.09 уже делал 64 загрузки за 2 ч 38 мин, то есть 50–66 укладываются в трёхчасовое окно. Если подход начнёт регулярно упираться в конец окна и терять остаток доли — вернуться к числам, а не наращивать дальше | 2026-09-14 |
